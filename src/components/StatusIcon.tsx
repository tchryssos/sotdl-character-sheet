interface ButtonStatusProps {
  isLoading?: boolean;
  isSuccessful?: boolean;
  hasError?: boolean;
}

export const ButtonStatus: React.FC<ButtonStatusProps> = ({
  isLoading,
  isSuccessful,
  hasError,
}) => {
  const something = '';
  return null;
};
